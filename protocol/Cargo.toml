[package]
name = "megadisplay-protocol"
description = "Wire protocol for MegaDisplay — compatible with SuperDisplay"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
thiserror = "2"
